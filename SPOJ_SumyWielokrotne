#include <iostream>

using namespace std;

int main()
{
    long long int a;
    long long int b = 0;
    long long int c = 0;
    while(cin>>a)
    {
        b += a;
        if(a==0)
        {
            cout<<b<<"\n";
            c += b;
            b = 0;
        }
    }
    cout<<c<< "\n";
}
